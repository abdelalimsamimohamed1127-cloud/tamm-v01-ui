export interface AutomationDTO {
  id: string;
  workspace_id: string;
  name: string;
  trigger_event: string;
  action_type: string;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}
